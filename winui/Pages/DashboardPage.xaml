<Page
    x:Class="OpenclawGuard.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollView>
        <StackPanel Spacing="16" Margin="32">
            <TextBlock Text="Dashboard" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock Text="Openclaw Gateway Monitor" Style="{ThemeResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

            <!-- Stat cards -->
            <ItemsControl ItemsSource="{x:Bind Stats}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="12"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="12" Padding="24" Width="200" Height="120"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                            <StackPanel Spacing="8">
                                <Ellipse Width="10" Height="10" Fill="{Binding Color}"/>
                                <TextBlock Text="{Binding Value}" FontSize="28" FontWeight="Light"
                                           Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                <TextBlock Text="{Binding Label}" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Quick actions -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="12" Padding="16"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="Quick Actions" FontWeight="SemiBold" VerticalAlignment="Center"
                               Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                    <Button Content="Start Gateway" Margin="12,0,0,0"/>
                    <Button Content="Stop Gateway"/>
                    <Button Content="Check Updates"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollView>
</Page>
